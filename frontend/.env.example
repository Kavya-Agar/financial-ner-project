# Base URL for the backend API. Leave empty (the default) when the Flask
# backend serves this app's built static files from the same origin
# (production). In local dev, point it at the backend dev server, e.g.:
# VITE_API_BASE_URL=http://localhost:5000
VITE_API_BASE_URL=
